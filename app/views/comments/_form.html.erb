<%= simple_form_for ([@micropost, @micropost.comments.build]) do |f| %>
  <%= f.hidden_field :commenter, value: current_user.name %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
   <%= f.button :submit, "Submit Comment" %>
  </p>
<% end %>